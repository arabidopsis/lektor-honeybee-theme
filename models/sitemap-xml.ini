[model]
name = Site map XML
protected = yes

[fields.changefreq]
label = Change frequency
default = monthly
type = select
choices = always, hourly, daily, weekly, monthly, yearly, never

[children]
enabled = no

[attachments]
enabled = no
