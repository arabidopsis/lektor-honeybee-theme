[model]
name = 404
protected = yes

[fields.info]
type = info
description = "This is the page displayed where there isn't a page to display so it's kind of special. It need to be very simple."

[fields.sorry]
label = Sorry Text
type = string
default = Sorry, but the page you were trying to view does not exist.
width = 5/6

[fields.color]
label = Text Color
type = string
input-type = color
default = #ffffff
width = 1/6

[children]
enabled = no

[attachments]
enabled = no
