[model]
name = Publications
label = {{ this.title }}
inherits = page
protected = yes


[children]
model=publication
order_by = -date
slug_format = {{ (this.date|dateformat('YYYY/M/') if this.date) ~ this._id }}